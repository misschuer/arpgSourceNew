#include <gtest/gtest.h>
#include <database/DatabaseEnv.h>


TEST(Player,player_test)
{
	//std::string mysqlInfo = "192.168.3.2;3306;dev;asdf;char";
	//g_sqlCharacter.Open(mysqlInfo);
	//
	//Player* player = new Player;
	//string name = "²Ëµ¶";
	////player->Create(1,name,1,1,1);
	////player->SaveToDB();
	//player->LoadFromDB(1);
	//string name_l = player->GetName();
	//EXPECT_EQ(name_l,"²Ëµ¶");
	//EXPECT_EQ(player->GetFaction(),1);
	//EXPECT_EQ(player->GetHead(),1);
	//EXPECT_EQ(player->GetGender(),1);
	//EXPECT_EQ(player->GetVipLv(),0);
	//EXPECT_EQ(player->GetPositionX(),50);
	//EXPECT_EQ(player->GetPositionY(),50);
	//EXPECT_EQ(player->Get_Level1_Attr((uint8)LEVEL1_ATTR_ROOT_BONE),10);
	//EXPECT_EQ(player->Get_Level2_Attr((uint8)LEVEL2_ATTR_DAMAGE),100);
	//EXPECT_EQ(player->GetUInt32Value(UNIT_FIELD_MOVESPEED),1200);

	//delete player;
}																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																																										
