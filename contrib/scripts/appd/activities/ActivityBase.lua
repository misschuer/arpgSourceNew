local ActivityBase = class("ActivityBase")

function ActivityBase:ctor()
	
end

-- 系统初始化
function ActivityBase:init(actId)
	
end

-- 系统结束
function ActivityBase:finish(actId)
	
end


-- 更新(目标, 领取状态)
function ActivityBase:update(playerInfo, actId, params)
	
end

-- 系统更新
function ActivityBase:systemUpdate(actId, params)
	
end

-- 玩家每日重置
function ActivityBase:reset(playerInfo, actId)
	
end



return ActivityBase